# Add functionality to determine the percentage that should be bet on games
# Add functionality to print output to a file
# Improve interface
# Use this project as an opportunity to learn Rust better?
